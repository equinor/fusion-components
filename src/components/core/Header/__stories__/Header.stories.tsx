import * as React from "react";
import { storiesOf } from "@storybook/react";
import Header from "../index";

storiesOf("Core components/Fusion header", module).add("Default", () => <Header />);
