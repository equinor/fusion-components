import React from 'react';
import { storiesOf } from '@storybook/react';

const HanginGarden: React.FC = () => {
    return <div>JOW</div>;
};

storiesOf('Data|Hanging Garden', module).add('Default', () => <HanginGarden />);
