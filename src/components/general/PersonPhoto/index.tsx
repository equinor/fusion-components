import React from 'react';
import styles from './styles.less';
import classNames from 'classnames';

export default () => {
    return <div className={classNames(styles.container)}>Person photo!</div>;
};
